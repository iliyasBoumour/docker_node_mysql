<div class="container">
   <a href="/addNew">Add Others</a>
</div>

<div class="table-users">
   <div class="header">Personnages</div>

   <table cellspacing="0">
      <tr>
         <th>photo</th>
         <th>nom</th>
         <th width="230">caracteristiques</th>
      </tr>
      {{#each pers}}
      <tr>
         <td><img src="{{{this.picture}}}" alt="" /></td>
         <td>{{{this.name}}}</td>
         <td>{{{this.car}}}</td>
      </tr>
      {{/each}}
   </table>
</div>